<%= simple_form_for [product, OrderItem.new], method: :post, html: { class: "d-inline" } do |f| %>
  <%= f.input :product_id, as: :hidden, input_html: { value: product.id } %>
  <%= f.input :quantity, as: :hidden, input_html: { value: 1 } %>

  <%= f.button :submit, "Add to Cart", class: "btn btn-mustard", style: "font-size: 18px; padding: 12px 24px; font-weight: bold;" %>
<% end %>
